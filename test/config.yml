schemas:
  - $id: test
    type: object
    properties:
      foo:
        type: string
        const: bar
    required: ["foo"]
routes:
  - route: /foo
    proxy:
      url: http://localhost:8080/bar
      timeout: 1000
    schema: test
